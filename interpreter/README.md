### intro

Python is used primarily for scripting, auotmating tasks, adding quick tests or anything where quick prototype is needed.

Reason being its high level, more expressive and **interpreted langauge**.
meaning code is trnaslated to **bytecode** first (closer to amchine language) then interpereted by CPython (interpretor).
In compiled languages like C, code is translated to machine code first then run.

Program is broken into **modules.**

